'use strict';
module.exports=({e,icon,origin})=>{
 const data=require('../content/scale-story.json');
 const html=`<section class="scale-story wrap" id="scale-for-every-child" aria-labelledby="scale-story-title"><div class="scale-story-top"><p class="eyebrow">THE SCALE BEHIND A PERSONAL JOURNEY</p><h2 id="scale-story-title">Built at scale.<br><em>Focused on your child.</em></h2><p>A connected system for understanding abilities, planning everyday practice and reviewing progress toward a more independent life.</p></div><div class="scale-story-equation"><div><span class="scale-story-number">${e(data.aggregate.display)}</span><h3>${e(data.aggregate.label)}</h3><p>${e(data.aggregate.status)} · <a href="#scale-story-basis">Counting basis</a></p></div><span class="scale-story-for">for</span><div><span class="scale-story-number">${e(data.mission.display)}</span><h3>${e(data.mission.label)}</h3><p>${e(data.mission.status)} · <a href="/evidence/records/september-handout.html">Published mission</a></p></div></div><p class="scale-story-scope">400 billion is Pinnacle’s aggregate estimate; its full calculation has not been independently reproduced here. 900 million is the mission population, not people already served.</p><p class="scale-story-families">${icon('users')} <strong>${e(data.families.value)} ${e(data.families.label)}</strong><a href="${e(data.families.source)}">Independent report finding ${icon('arrow-up-right')}</a></p><div class="scale-story-metrics">${data.metrics.map(m=>`<div class="scale-story-metric">${icon(m.icon)}<strong>${e(m.value)}</strong><h3>${e(m.label)}</h3><span>${e(m.status)}</span><p>${e(m.meaning)}</p><a href="${e(m.source)}">Source &amp; meaning ${icon('arrow-up-right')}</a></div>`).join('')}</div><div class="scale-story-human"><h3>All that scale should lead to a clearer next step for your child.</h3><p>The value is the connection: a personal goal, a chosen activity, guided practice, feedback and a plan that can change with your child.</p><ol>${data.journey.map(j=>`<li>${icon(j.icon)}<strong>${e(j.label)}</strong><span>${e(j.detail)}</span></li>`).join('')}</ol><a href="#how-pinnacleai-works">Follow the PinnacleAI® journey ${icon('arrow-up-right')}</a></div><details class="scale-story-basis" id="scale-story-basis"><summary>Read the counting basis and independent evidence ${icon('plus')}</summary><h3>Current figures and dated report findings</h3><p>${e(data.aggregate.basis)}</p><p>${e(data.mission.basis)}</p><dl>${data.metrics.map(m=>`<dt>${e(m.value)} · ${e(m.label)}</dt><dd>${e(m.basis)}</dd>`).join('')}</dl><p>${e(data.families.basis)}</p><p>${e(data.programmeDescription)}</p><p>${e(data.countingNote)}</p><p><strong>Independent practitioner evidence:</strong> SAE 3000 limited assurance and SRS 4400 factual findings, with separately defined scopes. Both UDINs were verified Active on 19 September 2026.</p><p>SAE 3000: <strong>26200027GMBLXU4868</strong><br>SRS 4400: <strong>26200027HCNFEI9138</strong></p><div class="scale-story-source-links"><a href="/evidence/assurance-map.html">Read the claim-to-report map</a><a href="/evidence/scale-and-mission.html">Open the shareable scale story</a><a href="/evidence/icai-udin-verifications-2026-09-19.pdf">Original ICAI verification receipt</a><a href="/evidence/scale-story.json">These figures as readable data</a></div><p>${e(data.comparisonScope)}</p></details><div class="scale-story-challenge"><p class="eyebrow">A BETTER QUESTION WHEN CHOOSING SUPPORT</p><h3>${e(data.comparisonQuestion)}</h3><a class="primary-link" href="#proof-map">See what Pinnacle can show you ${icon('arrow-up-right')}</a></div></section>`;
 const text='## '+data.title+'\n\n'+data.aggregate.display+' '+data.aggregate.label+' — '+data.aggregate.status+'. '+data.aggregate.basis+'\n'+data.mission.display+' '+data.mission.label+' — '+data.mission.status+'. '+data.mission.basis+'\n\n'+data.metrics.map(m=>m.value+' '+m.label+' — '+m.status+'\n'+m.basis+'\nSource: '+new URL(m.source,origin).href).join('\n\n')+'\n\n'+data.families.value+' '+data.families.label+'\n'+data.families.basis+'\n'+data.programmeDescription+'\n\n'+data.countingNote+'\n'+data.comparisonQuestion+'\n'+data.comparisonScope+'\n';
 // Keep the full shareable article; the homepage offers its detail on demand.
 let homeHtml=html.replace('class="scale-story wrap"','class="scale-story scale-story-home wrap"');
 homeHtml=homeHtml.replace('<p>A connected system for understanding abilities, planning everyday practice and reviewing progress toward a more independent life.</p>','');
 for(const m of data.metrics)homeHtml=homeHtml.replace(`<p>${e(m.meaning)}</p>`,'');
 homeHtml=homeHtml.replace('<div class="scale-story-human">',`<details class="scale-story-more"><summary>${icon('files')}How the figures connect · sources &amp; counting basis ${icon('plus')}</summary><div class="scale-story-human">`).replace('</section>','</details></section>');
 return {data,html,homeHtml,text};
};
