'use strict';
const data=require('../content/hfr-register.json');
module.exports=({e,icon})=>{
 const checked=data.dashboard.checkedOn,statuses=['Approved','Query Raised','Submitted','Rejected','Query Resolved'];
 const isApproved=r=>r.liveLookup.status==='Approved';
 const badge=r=>isApproved(r)?`<span class="badge matched hfr-approval">${icon('badge-check')}Approval verified</span>`:'<span class="hfr-recorded">HFR ID recorded</span>';
 const summary=`<div class="hfr-summary"><div><strong>${data.counts.liveMatchedIds}</strong><span>HFR identifiers included and source-reviewed</span></div><div><strong>${data.counts.approvedIds}</strong><span>Approved statuses confirmed in the reviewed account</span></div></div>`;
 const note='All 53 identifiers from the supplied workbook and certificate collection are included, alongside three additional dashboard IDs. Each of the 56 complete IDs was matched in the NHPR account reviewed on 19 September 2026. Identifiers are not a count of distinct operating centres.';
 const legend=`<div class="hfr-proof-key"><p><span class="badge matched">${icon('badge-check')}Approval verified</span> The tick confirms that this exact ID displayed <strong>Approved</strong> in the NHPR account reviewed on <time datetime="${checked}">19 September 2026</time>.</p><p><strong>Every supplied ID remains visible.</strong> Unticked IDs have their exact dated account observations under Source details. Pinnacle confirms its registration coverage is complete and advises that records may span multiple accounts. The dated portal check in this review covers one account; ticks identify the approval statuses confirmed in that check.</p></div>`;
 const sourceDetails=r=>`<details class="hfr-source-details"><summary>Source details<span class="sr-only"> for ${e(r.name)}, ${e(r.id)}</span></summary><div><dl><div><dt>NHPR workflow · reviewed account</dt><dd>${e(r.liveLookup.status)}</dd></div><div><dt>Observation</dt><dd><time datetime="${checked}">${checked}</time> · dashboard page ${r.liveLookup.page}</dd></div><div><dt>Documentary source</dt><dd>${r.workbookRow?`Workbook row ${r.workbookRow}<small>Original RS flag: ${e(r.sourceFlag)} (separate from the dashboard workflow)</small>`:r.certificate?'Recognitions certificate collection':'Additional dashboard identifier'}</dd></div>${r.certificate?`<div><dt>Inspected certificate copy</dt><dd>${e(r.certificate.date)} · Recognitions bundle p${e(r.certificate.pages)}</dd></div>`:'<div><dt>Certificate copy</dt><dd>Not part of the inspected source collection.</dd></div>'}</dl>${r.liveLookup.displayedName?`<p>Dashboard label: ${e(r.liveLookup.displayedName)}</p>`:''}<a href="/evidence/hfr-register.html#hfr-${e(r.id)}">Link to this HFR record</a></div></details>`;
 const rows=data.rows.map(r=>`<tr id="hfr-${e(r.id)}" data-hfr-status="${e(r.liveLookup.status)}" data-hfr-evidence="${isApproved(r)?'approved':'recorded'}" data-hfr-search="${e([r.name,r.id,r.address,r.postcode,r.liveLookup.displayedName,r.liveLookup.status].join(' ').toLowerCase())}"><th scope="row" data-label="Centre">${e(r.name)}</th><td data-label="HFR identifier"><strong class="hfr-id">${e(r.id)}</strong>${badge(r)}</td><td data-label="Address in source">${e(r.address)}${r.postcode?`<small>Source postcode: ${e(r.postcode)}</small>`:''}</td><td data-label="Source review">${sourceDetails(r)}</td></tr>`).join('');
 const draft=data.drafts[0];
 const content=`<p class="eyebrow">PINNACLE NETWORK · TRACEABLE FACILITY RECORDS</p><h1>Find your centre.<br>Follow its HFR record.</h1><p class="intro-copy">A searchable view of Pinnacle’s supplied Health Facility Registry identifiers, connected to their documentary sources and dated NHPR checks.</p>${summary}${legend}<div class="hfr-tools"><label for="hfr-search">Find a centre, address, postcode or HFR ID<input id="hfr-search" type="search" placeholder="Suchitra, Bengaluru, IN3610023010…"></label><label for="hfr-status">Evidence view<select id="hfr-status"><option value="all">All HFR IDs (${data.rows.length})</option><option value="approved">Approval verified (${data.counts.approvedIds})</option><option value="recorded">Other recorded IDs (${data.rows.length-data.counts.approvedIds})</option></select></label><p id="hfr-result" role="status" aria-live="polite">${data.rows.length} HFR records</p><a class="outline-link" href="/evidence/hfr-register.csv">${icon('download')}Download source register · CSV</a></div><div class="hfr-table"><table><caption>All ${data.rows.length} complete HFR identifiers. Source review: <time datetime="${checked}">19 September 2026</time>. Expand source details to read each account observation.</caption><thead><tr><th>Centre</th><th>HFR identifier</th><th>Address in source</th><th>Source review</th></tr></thead><tbody>${rows}</tbody></table></div><p id="hfr-empty" hidden>No matching records. Try another name, ID or evidence view.</p><details class="hfr-method hfr-notes" id="hfr-review-method"><summary>Review coverage &amp; original source details</summary><div><h2>What was reviewed</h2><p>${note}</p><p>All four pages of one authenticated NHPR facility-manager account were reviewed. This is a dated account observation, not a live feed or a review of every account held by Pinnacle.</p><p><strong>Original dashboard labels:</strong> ${statuses.map(s=>data.dashboard.statusCounts[s]+' '+s).join(' · ')} · 1 Draft. These are administrative workflow labels. They do not rate clinical quality, establish current operations or replace other premises-specific licences.</p><p>${e(data.scope)}</p><p>The original collection contains all 52 workbook entries and 28 distinct IDs with inspected certificate copies, reconciled to 53 identifiers. The dashboard adds Bhimavaram IN2810051839, Nizamabad IN3610023029 and Khammam IN3610023023. The 37 ACTIVE and 15 INACTIVE workbook RS values are preserved; their operational meaning was not supplied.</p><p><strong>Similar names, separate records:</strong> Tirupati, Bhimavaram, Khammam and Nizamabad each have two IDs in the reviewed dashboard. They are kept separately, without assuming replacement, cancellation or an additional physical centre. Karkhana is the dashboard label for the workbook’s Vikrampuri identifier IN3610023703. Karimnagar’s dashboard name field displays an address. Matching is by exact HFR identifier.</p><p>Addresses and postcode columns remain documentary source values. Confirm the premises with Pinnacle when choosing a centre.</p><section id="hfr-draft"><h3>Additional masked Draft</h3><p>${e(draft.name)} · ${e(draft.displayedId)} · Draft · dashboard page ${draft.page}. This entry is separate from the 56 complete identifiers and 33 Approved records. No missing digits have been inferred.</p><p>${e(draft.address)}</p></section><p><strong>Sources:</strong> ${e(data.source.workbook)}, sheet ${e(data.source.sheet)}, rows 2–53; Recognitions bundle, physical pages 67–95; NHPR facility-manager dashboard, checked 19 September 2026. The authenticated dashboard requires account access and is not a public per-facility lookup link.</p><div class="record-actions"><a href="https://hfr.abdm.gov.in/" target="_blank" rel="noopener noreferrer">${icon('external-link')}Official ABDM HFR portal</a><a href="/evidence/hfr-dashboard-checks.json">${icon('file-search')}Dated dashboard checks · JSON</a><a href="/evidence/hfr-register.json">${icon('files')}Complete register · JSON</a><a href="/evidence/records/hfr.html">${icon('file-search')}HFR evidence record</a></div></div></details>`;
 const centres=data.rows.map(r=>({kind:'hfr',name:r.name,identifier:r.id,date:r.certificate?r.certificate.date:'Source review: 19 September 2026',source:r.workbookRow?'All Centers With HFR.xlsx · row '+r.workbookRow:r.certificate?'Recognitions bundle · p'+r.certificate.pages:'NHPR dashboard · page '+r.liveLookup.page,note:isApproved(r)?'Approved status confirmed for this HFR ID in the NHPR account reviewed on 19 September 2026.':'HFR identifier included and source-reviewed. Open the record for its dated account observation and documentary sources.',registryStatus:r.liveLookup.status,checkedOn:checked,address:r.address,postcode:r.postcode,sourceLevel:r.sourceLevel,sourceUrl:'/evidence/hfr-register.html#hfr-'+r.id,search:[r.name,r.id,r.address,r.postcode,r.liveLookup.displayedName,r.liveLookup.status].join(' ').toLowerCase()}));
 const csvCell=s=>'"'+String(s??'').replaceAll('"','""')+'"';
 const csv=[['Centre','HFR identifier','Dashboard status','Dashboard check date','Dashboard page','Address in original source','Postcode in source','Workbook row','Workbook RS flag (undefined)','Certificate date','Certificate source pages','Source level'],...data.rows.map(r=>[r.name,r.id,r.liveLookup.status,checked,r.liveLookup.page,r.address,r.postcode,r.workbookRow,r.sourceFlag,r.certificate?.date,r.certificate?.pages,r.sourceLevel])].map(r=>r.map(csvCell).join(',')).join('\n')+'\n';
 return {data,summary,note,legend,content,centres,csv};
};
